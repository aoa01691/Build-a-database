#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>

// Simple key-value pair struct (our "index" entry)
struct Entry {
    std::string key;
    std::string value;
};
