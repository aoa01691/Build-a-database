# Build-a-database

# Simple Key-Value Store (Append-Only)

## Build
make

## Run
./kvstore

## Commands (STDIN -> STDOUT)
SET <key> <value>
GET <key>
EXIT

Outputs:
- SET: OK or ERROR
- GET: value or NULL
